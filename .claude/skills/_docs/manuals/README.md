# Manuals

汎用マニュアル類 (Git flow / Deploy / Rollback / Secrets / Troubleshooting) は **テンプレ側 (`vercel-aiagent-coding-skill-starter`) で `docs-runbook` Plugin が `/docs-runbook init` で生成**する設計。雛形ファイルは [plugins/docs-runbook/templates/](../../docs-runbook/templates/) を参照。

Marketplace 側 (この repo) に置く必要がある運用ドキュメントが出てきたらここに追加する。
