# CLAUDE.md

このプロジェクトのハウスルール (規約・サーフェス使い分け・Plugin 一覧・禁止事項など) は
`AGENTS.md` に集約しています。Claude Code は以下の import 構文でこの内容を自動取り込み:

@AGENTS.md

> **編集する場合は `AGENTS.md` を編集してください**。このファイル (`CLAUDE.md`) は
> import 用のスタブで、内容を直接書くことはありません。

## 確認方法

セッション内で「ハウスルールに従って◯◯して」と依頼したとき、`AGENTS.md` の内容
(プロジェクト概要、サーフェス使い分け、ディレクトリ規約、禁止事項など) が
反映されていれば import が効いている。効いていないようなら、`AGENTS.md` の内容を
そのままここに展開する運用 (= pre-commit hook で同期する方式) に切り替える。
